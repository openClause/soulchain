{
  "name": "soulchain",
  "private": true,
  "packageManager": "pnpm@10.30.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2",
      "esbuild",
      "better-sqlite3"
    ]
  }
}
