{
  "name": "@openclaused/cli",
  "version": "0.6.3",
  "description": "CLI for SoulChain — initialize, verify, restore, and manage blockchain-anchored identity",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "soulchain": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/paulingram/soulchain",
    "directory": "packages/cli"
  },
  "keywords": [
    "soulchain",
    "cli",
    "blockchain",
    "identity",
    "ai-agent"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@openclaused/core": "workspace:*",
    "@openclaused/openclaw": "workspace:*",
    "chalk": "^4.1.2",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
